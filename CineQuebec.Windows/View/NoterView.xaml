<Window x:Class="CineQuebec.Windows.View.NoterView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CineQuebec.Windows.View"
        mc:Ignorable="d"
        Title="NoterView"  Height="150" Width="300">
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Label Grid.Row="0" Content="Note:" Margin="5"/>
            <TextBox x:Name="RatingTextBox" Grid.Row="1" Margin="5" Text="{Binding Note.NoteValue}"/>
            <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="5">
                <Button x:Name="ConfirmButton" Content="Confirmer" Margin="0,0,10,0" Command="{Binding ConfirmCommand}"/>
                <Button x:Name="CancelButton" Content="Annuler" Margin="0,0,10,0" Click="CancelButton_Click" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
